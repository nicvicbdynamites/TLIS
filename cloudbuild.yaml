steps:
  # ---------------------------------------------------------
  # 1. Build Phase: Build Docker images for API and Frontend
  # ---------------------------------------------------------
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-api'
    args:
      - 'build'
      - '-t'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'
      - '-t'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'
      - '-f'
      - 'artifacts/api-server/Dockerfile'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-frontend'
    args:
      - 'build'
      - '-t'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'
      - '-t'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'
      - '-f'
      - 'artifacts/tiktok-luxury/Dockerfile'
      - '.'

  # ---------------------------------------------------------
  # 2. Push Phase: Push Docker images to Artifact Registry
  # ---------------------------------------------------------
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-api'
    args:
      - 'push'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-api-latest'
    args:
      - 'push'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-frontend'
    args:
      - 'push'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-frontend-latest'
    args:
      - 'push'
      - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'

  # ---------------------------------------------------------
  # 3. Deploy Phase: Deploy services to Cloud Run
  # ---------------------------------------------------------
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-api'
    args:
      - 'run'
      - 'deploy'
      - 'tlis-api'
      - '--image=europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'
      - '--region=europe-west2'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=8080'

  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-frontend'
    args:
      - 'run'
      - 'deploy'
      - 'tlis-frontend'
      - '--image=europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'
      - '--region=europe-west2'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=80'

images:
  - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'
  - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/api-server:latest'
  - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'
  - 'europe-west2-docker.pkg.dev/$PROJECT_ID/tlis/tiktok-luxury:latest'
options:
  logging: CLOUD_LOGGING_ONLY

